
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.2.9
  creationTimestamp: null
  name: remotenamespaces.catapult.thetechnick.ninja
spec:
  group: catapult.thetechnick.ninja
  names:
    categories:
    - vedette
    kind: RemoteNamespace
    listKind: RemoteNamespaceList
    plural: remotenamespaces
    shortNames:
    - rns
    singular: remotenamespace
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.claim.name
      name: Claim
      type: string
    - jsonPath: .spec.namespaceInRemoteCluster.name
      name: Namespace in Remote Cluster
      type: string
    - jsonPath: .status.phase
      name: Status
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: RemoteNamespace maps a
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: RemoteNamespaceSpec defines the desired state of RemoteNamespace.
            properties:
              claim:
                description: Back-Reference to the Claim binding to this RemoteNamespace.
                  Must be set if Bound.
                properties:
                  name:
                    type: string
                required:
                - name
                type: object
              namespaceInRemoteCluster:
                description: Reference to the Namespace in the RemoteCluster
                properties:
                  name:
                    type: string
                required:
                - name
                type: object
              reclaimPolicy:
                default: Delete
                description: What happens with this RemoteNamespace after it is released
                  from a Claim.
                type: string
              remoteCluster:
                description: RemoteCluster this namespace belongs to.
                properties:
                  name:
                    type: string
                required:
                - name
                type: object
              remoteNamespaceClass:
                description: Name of the provisioner this RemoteNamespace was assigned
                  to.
                type: string
            required:
            - remoteCluster
            type: object
          status:
            description: RemoteNamespaceStatus defines the observed state of RemoteNamespace.
            properties:
              conditions:
                description: Current conditions that apply to this instance.
                items:
                  properties:
                    lastTransitionTime:
                      description: LastTransitionTime is the last time the condition
                        transits from one status to another.
                      format: date-time
                      type: string
                    message:
                      description: Message is the human readable message indicating
                        details about last transition.
                      type: string
                    reason:
                      description: Reason is the (brief) reason for the condition's
                        last transition.
                      type: string
                    status:
                      description: Status is the status of the condition, one of ('True',
                        'False', 'Unknown').
                      type: string
                    type:
                      description: Type is the type of the RemoteNamespace condition.
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              observedGeneration:
                description: ObservedGeneration is the most recent generation observed
                  by the controller.
                format: int64
                type: integer
              phase:
                description: DEPRECATED. Phase represents the current lifecycle state
                  of this object. Consider this field DEPRECATED, it will be removed
                  as soon as there is a mechanism to map conditions to strings when
                  printing the property. This is only for display purpose, for everything
                  else use conditions.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
